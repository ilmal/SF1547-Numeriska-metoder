format long;

% Uppgift 3a
% -----------------------------
% Spänningen i kretsen ges av: 
% Lq''(t) + Rq'(t) + (1/C)q(t) = 0 
% q(0) = 1 
% dq/dt (0) = 0

% -> q'' = -((1/C)q - Rq')/L

% Strömmen ges av: 
% i = dq/dt

% q'' = f(t, q, q') 
% -> Q(t) = q(t), I(t) = q'(t)
% -> Q' = q' = , v' = q'' = f(t, q, q') = -((1/C)q - Rq')/L

U = [Q; I];
F = [   I; 
    -((1/C)*Q + R*I)/L]; % F(t, U)

U0 = [1; 0]; % U(t0) = [c1, c2]







